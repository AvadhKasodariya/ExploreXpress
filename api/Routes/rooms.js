import express from "express";
// import { Login, Registration } from "../controllers/authentication.js";

const router = express.Router();

// router.post("/Registration", Registration)
// router.post("/Login", Login)

export default router